# Schema

The Rust structs in `crates/executioner-core/src/protocol.rs` are currently the
authoritative protocol definition.

This directory is reserved for generated JSON Schema and OpenAPI artifacts once
the protocol stabilizes enough to support external SDK generation.
